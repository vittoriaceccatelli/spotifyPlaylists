# spotifyPlaylists
Automatically creates Spotify playlists from liked songs 

# Spotify Order

Automatically organize liked Spotify songs into playlists by decade.

## Features

- Groups liked songs by decade
- Creates Spotify playlists automatically
- Handles batch uploads
- Uses Spotify Web API via Spotipy

## Setup

```bash
pip install -r requirements.txt
